In order to obtain a gif of the simulated dynamic one must set the desired parameters within the main.py script before running it.
The gif is then generated and outputted within the same directory as the script.
